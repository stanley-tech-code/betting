name: AI Brain Trigger

on:
  schedule:
    - cron: '*/15 * * * *' # Run every 15 minutes
  workflow_dispatch: # Allow manual trigger

jobs:
  ping-brain:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up AI
        run: |
          curl -X POST https://boomdolla.co.ke/api/cron
